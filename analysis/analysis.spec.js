'use strict';

describe('myApp.analysis module', function() {

  beforeEach(module('myApp.analysis'));

  describe('analysis controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var analysisCtrl = $controller('analysisCtrl');
      expect(analysisCtrl).toBeDefined();
    }));

  });
});